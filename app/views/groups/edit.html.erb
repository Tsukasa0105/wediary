<% if @group.request_users.include?(current_user) && @group.invited_users.include?(current_user) %>

<%= render '/layouts/top_image', image_url: "/assets/undraw_team_spirit_hrr4.png" %>

  <div class="chat-group-form">
    <%= render partial: 'form', locals: { group: @group } %>
  </div>
<% end %>